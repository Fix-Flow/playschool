/**
 * @playschl/ui — Shared Component Library
 *
 * Reusable, themeable UI components shared across applications.
 * Built with React + vanilla CSS for maximum portability.
 */

// TODO: Export shared UI primitives here
// Example: export { Button } from './Button';
// Example: export { Input } from './Input';

export {};
